[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
log = { version = "0.4.21", features = ["std"] }
logger = { path = "../logger" }
raft = { path = "../raft" }
rpc = { path = "../rpc" }
tokio = { version = "1.36.0", features = ["rt", "net"] }
